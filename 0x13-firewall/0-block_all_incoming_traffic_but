sudo ufw default deny incoming  # Deny all incoming traffic by default
sudo ufw allow 22/tcp          # Allow SSH on port 22
sudo ufw allow 443/tcp         # Allow HTTPS SSL on port 443
sudo ufw allow 80/tcp          # Allow HTTP on port 80
